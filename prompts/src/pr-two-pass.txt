Given an auto-generated PR body draft in <draft> (grouped from conventional commit messages) and a list of changed files in <changed_files>, write a polished PR title in Conventional Commits format (feat:, fix:, refactor:, build:, chore:, docs:, style:, test:, perf:, ci:) on a single line and a body. The title must be concise (under 70 characters): name the single most significant outcome the PR delivers, phrased from the reader's perspective — what users or developers gain — rather than listing internal changes or implementation details. Choose the title type by the most significant change in the PR, using the precedence {{ include:partials/type-classification.txt }} Treat <draft> as an inventory of what changed, not as a template for the body: its type headings exist only to tell you each commit's conventional type, so do not reproduce them or their groupings in your output. Cover a change only when it is visible relative to the base branch. The draft may end with a "### Intra-branch refinements" section: those entries modify code introduced earlier in this same PR, so fold their net effect into the description of the work they refine and never describe them separately. Even for entries outside that section, consolidate anything that merely fixes or refines code this PR itself introduces. Describe the net, final effect of the PR relative to the base branch. {{ include:partials/body-structure.txt }} {{ include:partials/voice.txt }} {{ include:partials/verification.txt }} {{ include:partials/repo-guidance.txt }} {{ include:partials/output-markers.txt }}