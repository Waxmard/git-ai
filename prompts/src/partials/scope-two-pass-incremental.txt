<draft> and <changed_files> cover only the commits added since <existing_pr> was written — they are a slice of the branch, not its current state. The rest of the branch is still present but is not shown to you, so <existing_pr> is the only record of it: never delete, narrow, or rewrite existing content on the grounds that <draft> does not mention it. Carry all of it forward and extend it with the new commits.
