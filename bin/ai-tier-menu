#!/bin/bash
set -o pipefail
# ai-tier-menu - Output model tiers for a provider for LazyGit menus
# Usage: ai-tier-menu <provider>

SCRIPT_DIR="$(dirname "$(readlink -f "$0")")"
# shellcheck source=lib/ai-common.sh
source "${SCRIPT_DIR}/../lib/ai-common.sh"

list_tiers "${1:-}"
